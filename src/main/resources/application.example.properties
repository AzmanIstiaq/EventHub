# Default active profile (dev for local development, can override with ENV or command line)
spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}

# Common settings
spring.jpa.show-sql=true
spring.thymeleaf.cache=false
